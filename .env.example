# Tidio Configuration
# Get your public key from: Tidio Dashboard → Settings → Channels → Chat widget → Install
NEXT_PUBLIC_TIDIO_KEY=your_tidio_public_key_here

# Optional: Base URL for sitemap generation
NEXT_PUBLIC_BASE_URL=https://your-domain.com
